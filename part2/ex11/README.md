Mapbox provides a basic example for a React based Mapbox app: https://github.com/mapbox/mapbox-react-examples/tree/master/basic.

Now it is testable with `docker compose up`! Check http://localhost:3000/.
